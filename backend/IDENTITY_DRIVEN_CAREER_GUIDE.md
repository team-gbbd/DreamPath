# 정체성 중심 진로 상담 시스템 가이드

## 🎯 핵심 컨셉

**"너는 어떤 사람이야?"**

직업 추천이 아닌, 학생의 진로 정체성 확립을 목표로 하는 시스템입니다.

## 🌟 주요 특징

### 1. 4단계 대화 프로세스

```
1단계: 현재 (PRESENT) - "지금의 나"
   └─> 현재 감정 상태와 고민 파악

2단계: 과거 (PAST) - "경험 속의 나"  
   └─> 의미있었던 경험, 몰입의 순간 탐색

3단계: 가치관 (VALUES) - "내가 추구하는 것"
   └─> 삶에서 중요한 것, 이루고 싶은 것 발견

4단계: 미래 (FUTURE) - "되고 싶은 나"
   └─> 10년 후 모습, 지향하는 삶 그리기

5단계: 정체성 (IDENTITY) - "진짜 나"
   └─> 정체성 확립 및 구체적 진로 연결
```

### 2. 실시간 정체성 분석

- **지속적 분석**: 매 대화마다 정체성이 점진적으로 명확해짐
- **명확도 추적**: 0-100% 정체성 명확도 실시간 업데이트
- **인사이트 제공**: 대화 중 발견한 특징을 즉시 알려줌

### 3. 자동 단계 진행

- AI가 각 단계의 탐색이 충분한지 판단
- 자동으로 다음 단계로 자연스럽게 전환
- 학생은 단계를 의식하지 않고 대화만 하면 됨

## 🚀 API 사용법

### 1. 세션 시작

```bash
curl -X POST http://localhost:8080/api/chat/start \
  -H "Content-Type: application/json" \
  -d '{"userId": "student123"}'
```

응답:
```json
{
  "sessionId": "abc-123-def-456",
  "message": "안녕! 나는 너의 진로 정체성을 함께 찾아갈 상담사야. 편하게 이야기하자 😊"
}
```

### 2. 대화하기

```bash
curl -X POST http://localhost:8080/api/chat \
  -H "Content-Type: application/json" \
  -d '{
    "sessionId": "abc-123-def-456",
    "userId": "student123",
    "message": "요즘 진로 때문에 고민이 많아요"
  }'
```

응답:
```json
{
  "sessionId": "abc-123-def-456",
  "message": "진로 고민이 많구나. 무엇이 가장 마음에 걸려?",
  "role": "assistant",
  "timestamp": 1699876543210,
  "stageChanged": false,
  "identityStatus": {
    "currentStage": "현재",
    "stageDescription": "지금의 나",
    "overallProgress": 0,
    "clarity": 15,
    "clarityReason": "탐색 초기 단계. 학생의 현재 고민이 드러나기 시작함",
    "identityCore": "탐색 중...",
    "confidence": 10,
    "traits": [
      {
        "category": "감정",
        "trait": "진로 불안",
        "evidence": "진로 고민이 많다고 언급"
      }
    ],
    "insights": [
      "학생이 진로에 대한 불안을 느끼고 있음"
    ],
    "nextFocus": "구체적인 고민 내용 탐색 필요",
    "recentInsight": {
      "hasInsight": true,
      "insight": "진로에 대한 불안감이 느껴져요",
      "type": "감정"
    }
  }
}
```

### 3. 정체성 상태 조회

```bash
curl http://localhost:8080/api/identity/abc-123-def-456
```

응답:
```json
{
  "sessionId": "abc-123-def-456",
  "currentStage": "과거",
  "stageDescription": "경험 속의 나",
  "overallProgress": 25,
  "clarity": 45,
  "clarityReason": "의미있는 경험들이 드러나기 시작함",
  "identityCore": "창작을 통해 표현하는 것을 즐기는 사람",
  "confidence": 50,
  "traits": [
    {
      "category": "흥미",
      "trait": "창의적 활동 선호",
      "evidence": "그림 그리기에 몰입했던 경험"
    },
    {
      "category": "가치관",
      "trait": "자기 표현 중시",
      "evidence": "내 생각을 표현하는 게 중요하다고 언급"
    }
  ],
  "insights": [
    "창의적 표현에서 큰 만족감을 느낌",
    "혼자보다는 타인과 공유할 때 더 의미를 느낌"
  ],
  "nextFocus": "창작의 목적과 의미 더 탐색"
}
```

## 💡 대화 예시

### 초기 단계 (PRESENT)

```
AI: "안녕! 오늘 기분은 어때?"

학생: "약간 막막해요. 진로를 정해야 하는데 뭘 해야 할지 모르겠어요"

AI: "막막하구나. 진로를 정해야 한다는 부담이 크지?"

학생: "네, 친구들은 다 결정한 것 같은데 저만 못 정한 것 같아요"

AI: "다른 사람과 비교하니 더 불안해지는 거야. 그럼 지금까지 살면서 
     정말 몰입해서 뭔가를 한 경험이 있어?" 
     [→ 자연스럽게 2단계로 이동]
```

### 중기 단계 (VALUES)

```
AI: "지금까지 이야기 들어보니, 너는 창작을 통해 표현하는 걸 정말 
     좋아하는 것 같아. 그런데 혼자만의 만족보다는 다른 사람들과 
     공유할 때 더 큰 의미를 느끼는 것 같고"

학생: "맞아요! 그림을 그리는 것도 좋지만, 그걸 누군가 봐주고 
      반응해줄 때 더 뿌듯해요"

AI: "그렇다면 너에게 정말 중요한 건 '창작 그 자체'보다 
     '창작을 통한 연결'인 것 같은데, 어떻게 생각해?"
     [→ 정체성 핵심 발견]
```

### 후기 단계 (IDENTITY)

```
AI: "우리 지금까지 대화한 걸 정리해볼까?

     너는 **'창작을 통해 사람들과 연결되는 사람'**이야.
     
     혼자만의 예술보다는, 사용자의 경험을 더 나은 것으로 만들어서
     사람들의 삶에 긍정적 영향을 주는 데서 기쁨을 느끼는 사람이지.
     
     이런 너에게 어울리는 길은 여러 가지가 있어:
     - UX/UI 디자이너: 사용자 경험을 디자인
     - 브랜드 디자이너: 브랜드를 통해 사람들과 소통
     - 서비스 디자이너: 더 나은 경험 설계
     
     이 중에서 어떤 게 더 끌려?"
```

## 🎨 정체성 중심 vs 직업 중심

### ❌ 직업 중심 (기존)

```
"당신은 창의적이네요"
"흥미 분야는 디자인입니다"
→ "따라서 디자이너를 추천합니다"
```

**문제**: 정체성 없이 직업만 제시

### ✅ 정체성 중심 (개선)

```
"당신은 창작을 통해 사람들과 연결되는 사람이에요"
"혼자만의 작품보다 타인의 경험을 개선하는 데서 보람을 느끼죠"
→ "그런 당신이 빛날 수 있는 길로는..."
```

**장점**: 정체성이 먼저, 진로는 자연스러운 연결

## 📊 단계별 진행 기준

| 단계 | 최소 메시지 | AI 판단 기준 |
|------|-------------|-------------|
| PRESENT | 3개 | 현재 감정과 고민이 명확히 드러남 |
| PAST | 5개 | 의미있는 경험과 몰입 순간 발견 |
| VALUES | 5개 | 핵심 가치관이 드러남 |
| FUTURE | 5개 | 지향하는 미래 모습이 그려짐 |
| IDENTITY | - | 정체성 확립 및 진로 탐색 계속 |

## 🔧 기술 구조

```
ChatRequest
    ↓
CareerChatService
    ├─> CareerAssistant (단계별 질문 유도)
    └─> 단계 메시지 카운트 증가
    ↓
IdentityService
    ├─> shouldProgressToNextStage() (자동 단계 진행)
    └─> updateIdentityStatus() (실시간 정체성 분석)
        └─> IdentityAnalyzer
            ├─> assessClarity() (명확도 평가)
            ├─> extractIdentity() (정체성 추출)
            └─> generateInsight() (즉각 인사이트)
    ↓
ChatResponse + IdentityStatus
```

## 🎯 프론트엔드 통합 가이드

### 실시간 정체성 패널

```typescript
// 매 채팅 응답마다 identityStatus 수신
{
  message: "...",
  identityStatus: {
    currentStage: "과거",
    clarity: 45,
    identityCore: "탐색 중...",
    // ...
  }
}

// UI에 실시간 표시
<IdentityPanel>
  <Stage>{identityStatus.currentStage}</Stage>
  <Progress value={identityStatus.clarity} />
  <Identity>{identityStatus.identityCore}</Identity>
  {identityStatus.recentInsight?.hasInsight && (
    <Insight>{identityStatus.recentInsight.insight}</Insight>
  )}
</IdentityPanel>
```

### 인사이트 알림

```typescript
// 새 인사이트가 있으면 표시
if (response.identityStatus.recentInsight?.hasInsight) {
  showInsightNotification(
    response.identityStatus.recentInsight.insight
  );
}
```

### 단계 변경 애니메이션

```typescript
if (response.stageChanged) {
  animateStageTransition(
    response.identityStatus.currentStage
  );
}
```

## 📈 기대 효과

### 1. 학생 경험
- ✅ 자기 이해 깊어짐
- ✅ 진로 결정 확신 증가
- ✅ "나는 이런 사람이다" 명확화

### 2. 상담 품질
- ✅ 구조화된 대화 흐름
- ✅ 일관성 있는 정체성 확립
- ✅ 깊이 있는 탐색

### 3. 시스템 강점
- ✅ 1회성 분석 → 지속적 진화
- ✅ 직업 나열 → 정체성 기반 제안
- ✅ 정적 결과 → 실시간 성장

## 🚀 다음 단계

### Phase 1 (완료)
- [x] 4단계 대화 프로세스
- [x] 실시간 정체성 분석
- [x] 자동 단계 진행
- [x] 백엔드 API 구현

### Phase 2 (진행 예정)
- [ ] 프론트엔드 실시간 패널 UI
- [ ] 인사이트 알림 애니메이션
- [ ] 단계 전환 비주얼
- [ ] 정체성 진화 타임라인

### Phase 3 (향후)
- [ ] 정체성 기반 진로 DB 연결
- [ ] 멘토 매칭
- [ ] 진로 로드맵 생성
- [ ] 장기 추적 및 재상담

---

**"나는 누구인가?"를 발견하는 여정, DreamPath와 함께.**

작성일: 2025년 11월 14일  
버전: 1.0.0

