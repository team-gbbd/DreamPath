spring:
  application:
    name: dreampath-backend
  
  datasource:
    url: jdbc:postgresql://${DB_HOST:aws-1-ap-northeast-1.pooler.supabase.com}:${DB_PORT:5432}/${DB_NAME:postgres}?sslmode=require
    driver-class-name: org.postgresql.Driver
    username: ${DB_USER:postgres.ssindowhjsowftiglvsz}
    password: ${DB_PASSWORD:dreampath1118}
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8080

openai:
  api:
    key: ${OPENAI_API_KEY:your-api-key-here}
    model: gpt-4o-mini

python:
  ai:
    service:
      url: ${PYTHON_AI_SERVICE_URL:http://localhost:8000}

cors:
  allowed-origins: http://localhost:3000,http://localhost:5173,http://localhost:3002

logging:
  level:
    com.dreampath: DEBUG
    org.springframework.web: INFO



